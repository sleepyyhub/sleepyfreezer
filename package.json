{
  "name": "clovyre",
  "version": "0.1.0",
  "private": true,
  "description": "Clovyre \u2014 AI character chat",
  "scripts": {
    "setup": "npm run build && npm --prefix server run db:deploy",
    "build": "npm --prefix server install && npm --prefix web install && npm --prefix web run build",
    "start": "npm --prefix server run db:deploy && npm --prefix server start",
    "dev:api": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "test": "node server/test/parse.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
