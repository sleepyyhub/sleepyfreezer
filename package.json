{
  "name": "clovyre",
  "version": "0.1.0",
  "private": true,
  "description": "Clovyre — AI character chat",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix web install && npm --prefix server run db:push && npm --prefix server run db:seed",
    "build": "npm --prefix web run build",
    "start": "npm run build && npm --prefix server start",
    "dev:api": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "test": "node server/test/parse.test.js"
  }
}
