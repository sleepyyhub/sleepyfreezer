LUCK_fast_2.txt (uploaded)  vs  LUCK_fast.lua (current)

The uploaded file is BYTE-IDENTICAL to the original LUCK_fast.txt from the
start of this session. Same 3434 lines, no diff at all.

Measured the same way both times: the announcement is dispatched to every
connected handler in connection order, exactly as a client does, and timed from
the start of that dispatch to our invoke. Both builds forced to the same config
first (auto on, remote route, threshold 1).

==========================================================================
A. SignalBehavior WRITABLE  --  what my rig used to allow, and a live
   client does NOT. Both builds end up holding pole here.
==========================================================================
   uploaded   324   306   260 ns
   current    304   294   253 ns
   -> a wash. ~4% apart, inside the noise of this machine.

==========================================================================
B. SignalBehavior LOCKED  --  what a live client actually does. The place
   owns that property; a client cannot write it, so it stays Deferred.
==========================================================================
   uploaded   3751 ns   (4000 of 4000 announcements produced a send)
   current    265 ns   (4000 of 4000 announcements produced a send)
   uploaded   3202 ns   (4000 of 4000 announcements produced a send)
   current    246 ns   (4000 of 4000 announcements produced a send)

   -> about 11x. The uploaded build reports 'pole n/a': it never takes pole.
      Its Boost only takes pole when it reads SignalBehavior as Immediate,
      and on a live client it never will. So the game's own notification
      handler -- 3200 ns in this rig, more in the real game where it builds
      GUI and plays a sound -- runs in FRONT of its invoke, every time.

==========================================================================
C. Behaviour, locked run
==========================================================================
